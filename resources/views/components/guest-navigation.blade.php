<header class="bg-primary">
    <h1 class="flex justify-center text-white font-black text-3xl mb-10 pt-4">Het Goede Eten</h1>
    <nav class="flex justify-center space-x-4 pb-4">
        <a href="/login" class="text-white hover:text-secondary">{{ __('Log in') }}</a>
        <a href="#" class="text-white hover:text-secondary">{{ __('Menu') }}</a>
        <a href="#" class="text-white hover:text-secondary">{{ __('Book') }}</a>
    </nav>
</header>
